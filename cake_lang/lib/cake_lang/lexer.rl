%%{

  machine lexer;

}%%

module CakeLang
  class Lexer
    class << self
      %% write data;

      def lex(source)
        tokens = []

        %% write init;
        %% write exec;
        tokens
      end
    end
  end
end

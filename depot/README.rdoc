== Depot Application

This Application is a demo app from Agile Web development with Rails 4.

To run this application with some test data run:

  $ rake db:create
  $ rake db:schema:load
  $ rake db:seed

Then start the server with

  $ rails server

When requesting ```GET /products``` a list of sample products should be shown.

